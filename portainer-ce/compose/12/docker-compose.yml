version: "3.3"
services:
  coreDNS:
    image: coredns/coredns
    restart: always
    ports:
      - 53:53/udp
    volumes:
      - /media/butt/Arquivos/Docker/coreDNS/File:/root/
    command: --log-facility=-
networks:
  default:
    external:
      name: host